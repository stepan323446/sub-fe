import { Stack, styled } from "@mui/material";

export const StyledStack = styled(Stack)(() => ({
  marginBottom: 5,
}));

export const StyledAuthButtons = styled("div")(() => ({
  display: "grid",
  gridTemplateColumns: "145px 1fr",
  gap: "18px",
  marginBottom: "15px",
}));
export const StyledSignUpText = styled("div")(() => ({}));
